{
    "name": "browsersize.app",
    "module": "index.ts",
    "type": "module",
    "devDependencies": {
        "bun-types": "latest"
    },
    "scripts": {
        "staging": "bun --hot run index.ts --staging",
        "production": "bun run index.ts",
        "build": "rollup --config"
    },
    "version": "0.5.0",
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@rollup/plugin-terser": "^0.4.4",
        "hono": "^3.6.0",
        "rollup-plugin-generate-html-template": "^1.7.0"
    }
}
